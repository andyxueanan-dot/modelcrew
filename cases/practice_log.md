# 刷题记录 practice_log（ModelCrew 自练 scoreboard）

> 由 `/loop` 自动驱动：不断喂**全新题目**→ 8 角色跑 → Judge 打分 → 吸取经验写进 `references/` → 蒸馏获奖论文做法。
>
> **停止阈值（本轮 /loop 设定）**：当一道**全新题目**（非已有案例）经 **Judge ≥ 90/100** 且 **Critic 无 ❌**（无红线违反）即停——视为"框架已能稳定产出国一/O 奖级作品"。
>
> 每轮产物：新 `cases/practice_NN_*/` 工件 + 本表加一行 + `references/` 至少一处"吸取经验" + `winning_paper_patterns.md` 至少一条新蒸馏。

## 记分板

| # | 题目 | 题型 / 方法箱 | Judge | Critic | 吸取的教训（写进哪） | 蒸馏 |
|---|---|---|---|---|---|---|
| — | （基线）demo_dispatch_simpson | 数据型·因果/Simpson | 87 | 无❌ | 8角色端到端自跑=**纯净基准** | 弱效应招式箱 |
| 01 | 月度销量预测 | 预测型·时序(HW/OLS/季节朴素) | ~~86~~ ⚠️污染 | 无❌(2⚠️) | C4b「平局宣布赢家」；创新最弱→`innovation_boost`。**但解题由编排者亲写=污染，分数不算数** | `§3C` 时序得奖细则 |
| 02 | 传染病干预时机 | 机理型·SIR+干预(ODE) | ~~86.3盲评~~ ⚠️污染 | 无❌(独立RK4三方互验12/12) | C4c「阈值外推圆整数」；**评分泄漏致虚高89→盲评86**→`judge`加盲评铁律+必须看图 | `§3D` 机理/ODE得奖细则 |
| 02′ | 传染病干预时机（**纯净重跑**） | 机理型·SIR+干预(ODE) | **84（全无菌盲评）** | 无❌(9✅2⚠️,5视角8.8,独立RK4复算12/12) | 纯净84 < 污染盲评86.3 → 量化"编排者亲写"虚高~2.3；**新规"评委看图"当场抓出fig1标注圆整(300k vs 300466)不一致** | 已用`§3D`机理细则 |

## 🔒 纯净基准协议（iter2 用户纠偏后确立，刷题必须遵守）

基准要有效，**解题与评分都必须 clean-context**，否则量的是"带全上下文的编排者"而非"框架本身"：

1. **解题无菌**：建模/求解/写作交给**新开的 modelcrew 角色子代理**（analyst→modeler→solver→writer），各自 fresh context。编排者**只在角色间传递工件**，**绝不**亲手写 solve.py / 论文，**绝不**注入解法、数字、命名、或**从先前跑动学到的过程经验**（连"图要加中文字体回退""别用 R₀ 下标"这种踩坑 tip 都算泄漏——iter2 实证）。
2. **题面无菌**：README 只写情景 + 给定参数 + 问题，**不得**预埋方法/创新点（旧 iter2 README 曾把"阈值–窗口法+IWRR"写进题面=污染，已剔除）。
3. **评分无菌**：每次打分**全新 Judge + 中立 prompt**，不透露目标分/历史分/作者意图；**禁止续跑同一评委"重评提分"**（实测虚高 89 vs 盲评 86）。评委须 Read 打开 figures/*.png 真看图。
4. **污染隔离**：被污染的旧解归档到 `artifacts/_contaminated_*`（`_` 前缀=各角色跳过），并在 prompt 明令禁开。
5. **编排者本身有上下文**这一点不可完全消除——最彻底是连路由也交给无上下文 router；当前以"只传工件、不注入内容"为底线，并在本表如实标注任何残留泄漏。

> 当前状态：demo_dispatch=纯净；iter1/iter2(旧)=编排者亲写**已作废**；iter2′ 正按本协议重跑。

## 进度备注
- **阈值 90 未达（盲评 86）→ 继续。**
- 🔑🔑 **iter2 最大教训·评分诚信**：用户提醒"benchmark 必须单独开新 agent、不能带上下文"。实测验证：**带上下文续跑同一评委重评得 89，全新盲评只有 86（虚高 ~3 分）**。已把「**盲评铁律（无目标/历史分/作者意图泄漏）+ 评委必须 Read 打开 figures/*.png 看图**」写进 `agents/7_judge.md` + `skills/modelcrew-judge`（双同步）。**今后所有打分=全新 agent + 中立 prompt**。
- 🔑 **跨案例 meta-教训（续）**：创新性仍是系统性最弱维（demo_dispatch 7 / practice_01 7 / practice_02 8）。已强化 `innovation_boost.md`（命名+操作化稳健指标）；practice_02 已落地「阈值–窗口法 + IWRR」，创新维从 7 升到 8，方向对，但离 90 仍差表达/创新各半档。
- 工具升级：`check_frozen.py`/`check_paper_numbers.py` 改为**自动发现案例**，刷新题无需改工具。
- ⚠️ **待修**：`modelcrew-judge` 仅 Read/Write、过去未真正看图——已加规则要求看图，但需在后续 iter 验证规则生效。
